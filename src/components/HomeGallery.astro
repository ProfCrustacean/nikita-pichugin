---
import HomeFeedStage from "@components/HomeFeedStage";
import type { SiteContent } from "@lib/schema";

interface Props {
  gallery: SiteContent["homeGallery"];
}

const { gallery } = Astro.props;
---

<HomeFeedStage client:load gallery={gallery} />
