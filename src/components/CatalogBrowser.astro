---
import CatalogBrowserView from "../features/catalog/CatalogBrowserView.astro";
import type { MuseumWork } from "@lib/museum";

interface Props {
  works: MuseumWork[];
}

const { works } = Astro.props;
---

<CatalogBrowserView works={works} />
